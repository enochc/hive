# Mosquitto config for Hive MQTT testing
# Run with: mosquitto -c mosquitto.conf -v

# Listen on localhost only
listener 1883 127.0.0.1

# No authentication required for local testing
allow_anonymous true

# Logging to stderr so you see activity in the terminal
log_dest stderr
log_type all

# Keep connections responsive for testing
max_keepalive 60
