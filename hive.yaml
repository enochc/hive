
Properties:
  - is_active:
      type: "bool"
      default: true
  - light.value:
      type: "int"
  - thermostat.name:
      type: "string"
      default: "thermostat"
  - thermostat.temperature:
      type: "int"
  - thermostat.target_temp:
      type: "int"

#listen: 3000